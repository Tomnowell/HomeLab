---
- name: Install ECK
  hosts: control_plane

  tasks:

    - name: Install ECK CRDs
      kubernetes.core.k8s:
        state: present
        src: .crds.yaml

    - name: Install ECK operator
      kubernetes.core.k8s:
        state: present
        src: ./operator.yaml
